# Optional: enables live AI interpretation. The app uses a safe deterministic
# interpreter when this key is not configured, so the portfolio demo still works.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
